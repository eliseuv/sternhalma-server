[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "sternhalma-client"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.4.2",
]

[tool.hatch.build.targets.wheel]
packages = ["src/sternhalma_client"]
